# Mimir's Memory Hub — Python dependencies
# These are installed automatically by install.bat / run.sh

# ── Web server ───────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0          # includes websockets + httptools
python-multipart>=0.0.9            # file upload support (character import)

# ── HTTP client (LLM backends, HuggingFace downloads) ────────────────
httpx>=0.27.0

# ── Mimir memory engine ──────────────────────────────────────────────
vividmimir                          # core episodic memory system

# ── TTS ───────────────────────────────────────────────────────────────
edge-tts>=7.0.0                     # Edge TTS (default, no GPU needed)
transformers>=4.40.0                # model loading (Maya1 HF mode)
soundfile>=0.12.0                   # WAV encoding
snac>=1.2.0                         # SNAC audio codec
torch>=2.0.0                        # neural net inference

# ── STT (Whisper speech-to-text) ─────────────────────────────────────
faster-whisper>=1.0.0               # CTranslate2-based Whisper

# ── Optional: local GGUF model support ──────────────────────────────
# Uncomment the line below to enable running local .gguf files directly.
# For GPU acceleration you may need a CUDA-specific wheel — see:
# https://github.com/abetlen/llama-cpp-python/releases
#
# llama-cpp-python>=0.2.90
